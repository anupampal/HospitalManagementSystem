<UserControl x:Class="HospitalManagementSystem.Views.UserControls.DepartmentManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- Style for NavigationButton to resolve the errors -->
        <Style x:Key="NavigationButton" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#005C9A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005C9A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid Background="#F0F0F0">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0" Width="600">
            <TextBlock Text="🏢 Department Management"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0,0,0,20"
                       HorizontalAlignment="Center"/>

            <Grid Margin="0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Content="Name:" Grid.Row="0" Grid.Column="0" Margin="0,5"/>
                <TextBox x:Name="txtName" Grid.Row="0" Grid.Column="1" Height="30" Padding="5"/>

                <Label Content="Description:" Grid.Row="1" Grid.Column="0" Margin="0,5"/>
                <TextBox x:Name="txtDescription" Grid.Row="1" Grid.Column="1" Height="30" Padding="5"/>

                <Label Content="Location:" Grid.Row="2" Grid.Column="0" Margin="0,5"/>
                <TextBox x:Name="txtLocation" Grid.Row="2" Grid.Column="1" Height="30" Padding="5"/>

                <Label Content="Phone:" Grid.Row="3" Grid.Column="0" Margin="0,5"/>
                <TextBox x:Name="txtPhone" Grid.Row="3" Grid.Column="1" Height="30" Padding="5"/>

                <Label Content="Head of Dept:" Grid.Row="4" Grid.Column="0" Margin="0,5"/>
                <TextBox x:Name="txtHeadOfDept" Grid.Row="4" Grid.Column="1" Height="30" Padding="5"/>

                <Label Content="Budget:" Grid.Row="5" Grid.Column="0" Margin="0,5"/>
                <TextBox x:Name="txtBudget" Grid.Row="5" Grid.Column="1" Height="30" Padding="5"/>

                <Label Content="Is Active:" Grid.Row="6" Grid.Column="0" Margin="0,5"/>
                <CheckBox x:Name="chkIsActive" Grid.Row="6" Grid.Column="1" VerticalAlignment="Center" Margin="5"/>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button x:Name="btnAdd" Content="Add Department" Margin="5,0" Click="AddDepartmentButton_Click" Style="{StaticResource NavigationButton}"/>
                <Button x:Name="btnUpdate" Content="Update Department" Margin="5,0" Click="UpdateDepartmentButton_Click" Style="{StaticResource NavigationButton}"/>
                <Button x:Name="btnDelete" Content="Delete Department" Margin="5,0" Click="DeleteDepartmentButton_Click" Style="{StaticResource NavigationButton}"/>
            </StackPanel>

            <TextBlock Text="Existing Departments"
                       FontSize="16"
                       FontWeight="Bold"
                       Margin="0,40,0,10"
                       HorizontalAlignment="Center"/>
            <DataGrid x:Name="DepartmentsDataGrid"
                      Height="200"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      ItemsSource="{Binding Departments}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}"/>
                    <DataGridTextColumn Header="Location" Binding="{Binding Location}"/>
                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}"/>
                    <DataGridTextColumn Header="Head of Dept" Binding="{Binding HeadOfDept}"/>
                    <DataGridTextColumn Header="Budget" Binding="{Binding Budget}"/>
                    <DataGridCheckBoxColumn Header="Is Active" Binding="{Binding IsActive}"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
