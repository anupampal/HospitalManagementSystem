<UserControl x:Class="HospitalManagementSystem.Views.UserControls.BackupMaintenanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

	<UserControl.Resources>
		<!-- Self-contained style for all buttons in this view -->
		<Style x:Key="NavigationButton" TargetType="Button">
			<Setter Property="Background" Value="#1abc9c"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="BorderBrush" Value="#16a085"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Padding" Value="10,5"/>
			<Setter Property="HorizontalContentAlignment" Value="Center"/>
			<Setter Property="VerticalContentAlignment" Value="Center"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
							<ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="ButtonBorder" Property="Background" Value="#16a085"/>
							</Trigger>
							<Trigger Property="IsPressed" Value="True">
								<Setter TargetName="ButtonBorder" Property="Background" Value="#148f77"/>
								<Setter Property="RenderTransform">
									<Setter.Value>
										<ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
									</Setter.Value>
								</Setter>
								<Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</UserControl.Resources>

	<Grid Background="#F0F0F0">
		<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="500">
			<TextBlock Text="💾 Backup &amp; Maintenance"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0,0,0,20"
                       HorizontalAlignment="Center"/>

			<!-- Backup Settings -->
			<Grid Margin="0,10,0,10">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>

				<Label Grid.Row="0" Grid.Column="0" Content="Backup Location:" Margin="0,5"/>
				<TextBox Grid.Row="0" Grid.Column="1" x:Name="txtBackupLocation" Height="30" Padding="5"/>

				<Label Grid.Row="1" Grid.Column="0" Content="Backup Frequency:" Margin="0,5"/>
				<ComboBox Grid.Row="1" Grid.Column="1" x:Name="cmbBackupFrequency" Height="30" Padding="5">
					<ComboBoxItem Content="Daily"/>
					<ComboBoxItem Content="Weekly"/>
					<ComboBoxItem Content="Monthly"/>
				</ComboBox>

				<Label Grid.Row="2" Grid.Column="0" Content="Retention Period (days):" Margin="0,5"/>
				<TextBox Grid.Row="2" Grid.Column="1" x:Name="txtRetentionPeriod" Height="30" Padding="5"/>

				<Label Grid.Row="3" Grid.Column="0" Content="Email Notifications:" Margin="0,5"/>
				<TextBox Grid.Row="3" Grid.Column="1" x:Name="txtEmailNotifications" Height="30" Padding="5"/>

				<Label Grid.Row="4" Grid.Column="0" Content="Backup Time:" Margin="0,5"/>
				<TextBox Grid.Row="4" Grid.Column="1" x:Name="txtBackupTime" Height="30" Padding="5"/>
			</Grid>

			<!-- Buttons -->
			<Button Content="Run Backup Now" x:Name="btnRunBackupNow" Margin="0,10" Style="{StaticResource NavigationButton}"/>
			<Button Content="Optimize Database" x:Name="btnOptimizeDatabase" Margin="0,10" Style="{StaticResource NavigationButton}"/>
			<Button Content="Check Database Integrity" x:Name="btnCheckIntegrity" Margin="0,10" Style="{StaticResource NavigationButton}"/>

			<TextBlock x:Name="txtLastBackup" Text="Last Backup: 2025-09-24 10:00 AM" Margin="0,20,0,0" HorizontalAlignment="Center"/>
		</StackPanel>
	</Grid>
</UserControl>
