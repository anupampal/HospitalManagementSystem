<UserControl x:Class="HospitalManagementSystem.Views.UserControls.SupplierManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <!-- Style for form labels -->
        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#34495e"/>
            <Setter Property="Margin" Value="0,10,0,2"/>
        </Style>

        <!-- Style for form textboxes -->
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="#bdc3c7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#2ecc71"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#2ecc71"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Self-contained style for all buttons in this view -->
        <Style x:Key="CrudButton" TargetType="Button">
            <Setter Property="Background" Value="#2ecc71"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#27ae60"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#27ae60"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#229954"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F8F8F8" Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FFFFFF" Padding="20" CornerRadius="8"
                Margin="0,0,0,20">
            <TextBlock Text="🚚 Supplier Management"
                       FontSize="28"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Foreground="#34495e"/>
        </Border>

        <!-- Main Content Grid -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Side: CRUD Form in a card -->
            <Border Grid.Column="0"
                    Background="#FFFFFF"
                    CornerRadius="8"
                    Padding="20"
                    Margin="0,0,20,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Supplier Details"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Margin="0,0,0,15"
                                   Foreground="#2c3e50"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Supplier ID and Company Name -->
                            <StackPanel Grid.Column="0" Margin="0,0,15,0">
                                <Label Content="Supplier ID:"/>
                                <TextBox x:Name="txtSupplierId" IsReadOnly="True" Background="#F0F0F0" HorizontalAlignment="Stretch" MinWidth="120"/>
                                <Label Content="Contact Person:"/>
                                <TextBox x:Name="txtContactPerson"/>
                                <Label Content="Email:"/>
                                <TextBox x:Name="txtEmail"/>
                                <Label Content="City:"/>
                                <TextBox x:Name="txtCity"/>
                                <Label Content="Payment Terms:"/>
                                <TextBox x:Name="txtPaymentTerms"/>
                                <Label Content="Notes:"/>
                                <TextBox x:Name="txtNotes" Height="80" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <Label Content="Company Name:"/>
                                <TextBox x:Name="txtCompanyName"/>
                                <Label Content="Phone:"/>
                                <TextBox x:Name="txtPhone"/>
                                <Label Content="Address:"/>
                                <TextBox x:Name="txtAddress" Height="40" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                                <Label Content="Country:"/>
                                <TextBox x:Name="txtCountry"/>
                                <Label Content="Rating:"/>
                                <TextBox x:Name="txtRating"/>
                                <CheckBox x:Name="chkIsActive" Content="Is Active" Margin="0,15,0,0" VerticalAlignment="Center" Foreground="#34495e" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Grid>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                            <Button x:Name="btnAdd" Content="Add Supplier" Style="{StaticResource CrudButton}" Click="btnAdd_Click"/>
                            <Button x:Name="btnUpdate" Content="Update" Style="{StaticResource CrudButton}" Click="btnUpdate_Click" IsEnabled="False"/>
                            <Button x:Name="btnDelete" Content="Delete" Style="{StaticResource CrudButton}" Background="#e74c3c" BorderBrush="#c0392b" Click="btnDelete_Click" IsEnabled="False"/>
                            <Button x:Name="btnClear" Content="Clear" Style="{StaticResource CrudButton}" Background="#95a5a6" BorderBrush="#7f8c8d" Click="btnClear_Click"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Right Side: DataGrid in a card -->
            <Border Grid.Column="1"
                    Background="#FFFFFF"
                    CornerRadius="8"
                    Padding="20">
                <StackPanel>
                    <TextBlock Text="Existing Suppliers"
                               FontSize="20"
                               FontWeight="Bold"
                               Margin="0,0,0,15"
                               Foreground="#2c3e50"/>
                    <DataGrid x:Name="SuppliersDataGrid"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              ItemsSource="{Binding}"
                              SelectionChanged="SuppliersDataGrid_SelectionChanged"
                              Background="#FFFFFF"
                              BorderBrush="#E0E0E0"
                              BorderThickness="1"
                              AlternatingRowBackground="#f5f5f5">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Supplier ID" Binding="{Binding SupplierId}" Width="Auto"/>
                            <DataGridTextColumn Header="Company Name" Binding="{Binding CompanyName}" Width="*"/>
                            <DataGridTextColumn Header="Contact Person" Binding="{Binding ContactPerson}" Width="*"/>
                            <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="*"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                            <DataGridTextColumn Header="City" Binding="{Binding City}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
