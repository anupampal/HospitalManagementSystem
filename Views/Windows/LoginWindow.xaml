<Window x:Class="HospitalManagementSystem.Views.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HMS Login" Height="400" Width="350"
        WindowStartupLocation="CenterScreen">

    <StackPanel Margin="30" Background="White">
        <TextBlock Text="Hospital Management System" FontSize="20" 
                  HorizontalAlignment="Center" Margin="0,20"/>

        <TextBlock Text="Username" Margin="0,20,0,5"/>
        <TextBox x:Name="UsernameTextBox" Height="30"/>

        <TextBlock Text="Password" Margin="0,10,0,5"/>
        <PasswordBox x:Name="PasswordBox" Height="30"/>

        <CheckBox x:Name="RememberMeCheckBox" Content="Remember Me" Margin="0,10"/>

        <Button x:Name="LoginButton" Content="LOGIN" Height="40" Margin="0,20" 
                Background="Blue" Foreground="White" Click="LoginButton_Click"/>

        <TextBlock x:Name="ErrorMessageText" Foreground="Red" 
                  HorizontalAlignment="Center" Margin="0,10"/>

        <ProgressBar x:Name="LoadingProgressBar" Height="5" Margin="0,10" 
                    Visibility="Collapsed"/>
    </StackPanel>
</Window>